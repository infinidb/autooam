#!/bin/bash

if grep "infinidb start" /etc/rc.local > /dev/null; then
  echo "infinidb start command already in /etc/rc.local!"
else 
  echo "su - <%= @idbuser %> -c '<%= @idbinstalldir %>/bin/infinidb start'" >> /etc/rc.local
fi

sed -i 's/^exit 0$/# exit 0/' /etc/rc.local 
